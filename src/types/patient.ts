// Patient Status Enum (baseado no Prisma)
export enum PatientStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  ARCHIVED = "ARCHIVED",
}

// Gender Enum
export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
  OTHER = "OTHER",
  PREFER_NOT_TO_SAY = "PREFER_NOT_TO_SAY",
}

// Biological Sex Enum
export enum BiologicalSex {
  MALE = "MALE",
  FEMALE = "FEMALE",
}
